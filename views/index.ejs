<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Visa Concierge Intake</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="container">
    <div class="hero">
      <div>
        <span class="badge">Concierge Intake</span>
        <h1>Get visa monitoring without the chaos</h1>
        <p class="subtitle">Share your targets once. We monitor, alert, and optionally assist booking when matching slots appear.</p>
      </div>
      <a class="btn secondary" href="/admin/login">Admin</a>
    </div>

    <% if (ok) { %><p class="ok">Submitted successfully. We’ll contact you soon.</p><% } %>

    <div class="card">
      <form method="post" action="/intake">
        <div class="grid">
          <div>
            <label>Full name</label>
            <input name="full_name" required />
          </div>
          <div>
            <label>Time zone</label>
            <input name="timezone" placeholder="America/Chicago" />
          </div>
          <div>
            <label>Contact channel</label>
            <select name="contact_channel"><option>WhatsApp</option><option>Telegram</option><option>Email</option><option>SMS</option></select>
          </div>
          <div>
            <label>Contact handle/number</label>
            <input name="contact_handle" required />
          </div>
          <div class="row-2">
            <label>Portal URL</label>
            <input name="portal_url" value="https://ais.usvisa-info.com/" required />
          </div>
          <div>
            <label>Portal username/email</label>
            <input name="username" required />
          </div>
          <div>
            <label>Portal password</label>
            <input name="password" required />
          </div>
          <div>
            <label>Target cities</label>
            <input name="target_cities" placeholder="Mexico City, Guadalajara" required />
          </div>
          <div>
            <label>Target months</label>
            <input name="target_months" placeholder="February, March" required />
          </div>
          <div class="row-2">
            <label><input type="checkbox" name="auto_book" /> Auto-book matching slots</label>
          </div>
          <div class="row-2">
            <label>Notes</label>
            <textarea name="notes" rows="4"></textarea>
          </div>
        </div>
        <div class="actions">
          <span class="small">Not affiliated with any government agency.</span>
          <button class="btn" type="submit">Submit Intake</button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
